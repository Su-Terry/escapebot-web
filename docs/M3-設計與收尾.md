# EscapeBot Phase 2 — Milestone 3：設計與收尾

> 包子貓循環 + 貓 LLM 化身。**M3 已上線**，投擲後續改成真 3D 深度鎖定。
> 本檔 = M3 的現狀紀錄（前半）+ 原始設計稿摘要與開放問題的後續答案（附錄）。
> 爆點：把「玩家 input → LLM 處理 → LLM output」物理化成「捏包子 → 餵貓 → 貓吃 → 貓吐 → 撿拾」。貓 = LLM 擬人化。

---

## 🆕 3D 縱深改造（最新狀態）

M3 上線後，把投擲從「2D 側視 + 假縱深縮放」整個改成**真 3D 深度鎖定**，解掉早期「包子直上直下、原地縮小、看起來假」的問題。

- **真透視投影**：固定相機把 3D 世界座標 (x,y,z) 投影成螢幕座標，近大遠小自動成立（取代手刻的 depthOf/perspScale）。彈弓在前景、貓在遠處，包子真的往畫面深處飛向貓、一路縮小。
- **深度鎖定**：貓固定在一個深度平面；包子的深度速度自動求解，使其在到達貓高度的同一刻到達貓的深度 → 深度永遠不會失手。玩家只瞄**左右 + 力道**，深度系統包辦——把「平面螢幕上瞄深度」這個最難的問題從根繞掉。力道不足（弧頂搆不到貓高度）→ 判短少、彈回。
- **飛行純 analytic 3D**（不經 matter.js）；**命中改 3D 距離判定**（球形，取代 sensor body + 碰撞事件）。
- **落地散落**：包子落地後在地面 XZ 平面用 matter.js 自然散開（gravity 關掉，靠初速 + friction），近大遠小由投影決定。
- **場景**：全寬地面（取代早期梯形/窄地面），縱深靠真投影 + 貓遠小撐。彈弓前景置中、貓遠處置中（用實測 PNG 內容中心對齊，非 sprite 框）。

驗收（實玩）：不假、shoot 手感好、命中度好、狀態都接上。

---

## ✅ 早期完成（M3.1b + M3.2 核心，3D 改造前）

從一張貓草圖到能玩通一場的完整爆點：

- **核心循環接真 engine**：捏包子(打字+捏)→ 拉彈弓瞄準 → 命中貓 → 貓吃/嚼/吐(= 真 submitAction，LLM 處理)→ 回覆包子落地 → 點選撿讀真 narration。全程接真 engine，能玩到通關 + 接上分享卡。
- **憤怒鳥式拖曳瞄準**：拉彈弓(往後拉定方向+力道)、橡皮筋視覺、軌跡預測(解析公式，跟物理一致)、命中時貓高亮、能丟歪要練瞄準。
- **物理 / 散落**：回覆包子落地自然散開堆積、互不重疊(治本，取代固定 slot)。滿地包子是設計的一部分(不清理)。（註：飛行與命中的物理在 3D 改造後已改為 analytic + 3D 距離判定，見上方；matter.js 現只用於落地散落。）
- **場景**：背景漸層 + 暖土色地面 + 接地陰影，脫離 unreal。所有東西(貓/包子/彈弓)貼地。
- **描邊標最新回覆**：最新吐出的包子有亮黃描邊(持久，下一顆吐出才轉移)，在散落堆積中一眼找到最新那顆。
- **射歪飛回彈弓**：射歪/沒命中的玩家包子飛回彈弓重瞄(保留輸入文字)。結構預留(returnToLaunch 獨立函式)，之後接貓尾巴。
- **點選/軌跡準確**：hit area 對齊視覺內容(非 sprite 整框)、半徑對應視覺大小。

關鍵架構決定：PixiJS ticker 統一驅動(不用 Matter.Runner)、混合架構(PixiJS canvas + HTML input)、canvas 寬度用 ResizeObserver(解 flex 佈局寬度時序)。

---

## ⭐ 優先目標：LLM 提前處理輸入 (latency 掩護)

**這是優化不是 bug，但列為優先目標。**（同見缺口分析「地基」）

現況：LLM 在「包子命中貓」之後才 call submitAction(3-6s latency)，玩家在「捏→瞄準→射出」這段操作時間 LLM 卻在乾等。

優化：捏包子的瞬間就在背景提前 call submitAction，結果暫存在那顆包子上(bun.pendingResult)，命中貓時直接取用——把玩家操作的時間拿來掩護 LLM latency，等餵到貓嘴回覆通常已準備好，貓幾乎不用等就吐。

設計(待確認實作)：
- 捏 → 包子出現 + 背景 call submitAction → 結果暫存 bun.pendingResult
- (玩家瞄準/射出/射歪飛回重射 — 這段 LLM 在處理)
- 命中貓 → 已好: 象徵性嚼一下立刻吐 / 沒好: 繼續嚼等 pendingResult 好了再吐
- 射歪飛回重射 → 同顆包子同 pendingResult(輸入沒變, 不重新 call)

要處理的時序：提前 call 時機(捏的瞬間)、射歪重射復用暫存結果(不浪費 LLM call)、結果綁定到那顆包子命中時取用、input 禁用確保不會多顆同時提前處理。

---

## 🔧 M3 bug 狀態

### 已修
- **描邊偏移**：最新包子描邊在移動時偏移、跟包子對不上 → 在 2.5D→3D 改造中一併重做對齊，已修。
- **落地包子浮空**：包子本體浮在影子上方 → offset 家族問題（與早期浮空/描邊同源）。查 PNG 量出圖案視覺底部 row 51 vs anchor row 50，落地點取真地面 sy 再減 1×scale 對齊，spitBun 落點與 on_ground sync 同一公式（落地不跳變），已修。

### 待確認
- hover 提示（待丟包子 hover 放大、地上包子 hover 高亮）不確定做了沒，要確認。

---

## 🎯 操作分流設計

目標：減少 UX 負擔。不同操作各得其所，不是全部打字餵貓：

| 操作 | 怎麼做 | 狀態 |
|------|--------|------|
| 查看物品 | prefill 建議 → 點 → 餵貓 | 下一波做(prefill) |
| 移動/換房間 | 建築平面圖點地點(快捷鍵叫出 tap UI) | 下一個大方向 |
| 解謎/對話 | 捏包子餵貓 | 已做(核心爆點) |

解決最初痛點「全部打字餵貓太繁瑣」——高頻輕量的(查看/移動)簡化，核心好玩的(解謎/對話)保留餵貓儀式。

### 下一波 (確定做、低風險)
- **prefill 輸入建議 (copilot 式)**：用 engine 現有的 sceneItems/exits/inventory 資料生成輸入建議(「查看{物品}」「前往{出口}」「使用{背包物品}」)。減打字+認知負擔。形式待定(建議列表 vs 灰字 autocomplete vs 混合)。規則式(不用 LLM、不劇透)。
- **LLM 提前處理** (見上方 ⭐)：跟 prefill 有綜效。

### 下一個大方向 (升級、中大型)
- **建築平面圖 + 箭頭移動**：解「移動不直覺」。建築平面圖風格(俯視/房間是框/線條風)，小地圖常駐 + 鍵盤快捷鍵叫出可點的 tap UI 點地點移動。engine 已有 connectedLocationIds/出口資料可驅動。

### M3 後續 milestone (增強)
- **M3.2.5 貓尾巴**：射歪的包子貓甩尾巴掃回。接現有「飛回彈弓」結構(returnToLaunch 換尾巴動畫)。需貓尾巴素材。
- **M3.3 包子分類視覺**：黃金包(線索)/腐爛包(weird)/普通。誰標待定(見附錄 Q2)。需素材。
- **M3.4 包子整理/收藏**：玩家自選上色、收藏/線索牆/回顧(呼應 BACKLOG F3)。

### 視覺風格北極星
**順眼 + LLM 擬人化**。不追求華麗，順眼即可(現在暖色 pixel 風已達到)。核心是貓=LLM 化身的概念立得住。手繪素材之後補，風格基準已定。

---

## 🎨 待補素材 (手繪，目前用 placeholder)
- 貓的咬/嚼/吐 frames (現在用 cat-idle ↔ cat-hit 換圖 + 縮放模擬)
- 貓尾巴動作 (M3.2.5)
- 黃金包 / 腐爛包 sprite (M3.3)
- 真正的場景背景 (現在程式畫的漸層+色塊，結構已分圖層 bgLayer/groundLayer/shadowLayer 方便替換)

---

## ⚙️ Engine 待辦 (非 M3，但 M3 期間撞到/處理；詳見 BACKLOG)

### 已修(待新場景驗證)
- **謎題提示 in-game-only**：LLM 解謎提示不再引入遊戲外知識或自創規則。turnHandler hint 加 CRITICAL 段落限制只能用 WorldState 資訊。兩版同步。← 待新場景多玩幾場驗證。

### 待做 (修 1，需先想清楚設計)
- **Generator 謎題可解性**：generator 設 solution 但不驗證「場景線索能否推出答案」→ 謎題可能不可解。validateScenarioLogic 目前只查 deadlock。需加 solvability 檢查，有「充分指向 vs 直接寫答案(F-spoil)」兩難。核心技術資產，值得專門設計。（缺口分析軸 B 的根、Phase 3 因果圖徹底解。）

### 之前累積的 generator/engine 品質 bug (集中一批處理，詳見 BACKLOG)
F-spoil、容器鎖、重複顯示 item、位數描述不符、F11 誤演線索、鑰匙語意不一致、DB ECONNRESET（已加 isTransientDbError + withDbRetry 連線類錯誤重試 + 友善錯誤；註：當初的「production 反覆失敗」是從一張舊截圖誤判的，實際沒反覆失敗，retry 留作無害防禦，別再為它投入）等。

---

## 附錄：原始設計稿摘要 + 開放問題的後續答案

> 以下整理自建置前的 M3 原始規格。**開放問題 Q1–Q5 當時未解，這裡補上後續的答案**（散在後續設計裡，這裡集中）。

### 當初「已確認的決定」
| 項目 | 決定 |
|------|------|
| 丟包子 | 玩家手動（拖曳/甩） |
| 貓咬包子 | 加咀嚼動畫（手繪 frame，待製作） |
| 貓吐出 | 一顆（一則回覆一顆包子） |
| 包子分類 | 黃金=線索 / 腐爛=weird / 普通=一般 |
| tap UI | 先移除，回到打字（捏包子）為主 |

### 開放問題 Q1–Q5 與後續答案
- **Q1. tap 移除後，移動/解謎/拿物品怎麼操作？**
  → 後續答案：**操作分流**（見上）。解謎/對話=捏包子；查看=prefill 建議；移動=建築平面圖。不是全部打字。
- **Q2. 包子分類（黃金/腐爛/普通）誰決定？**
  → 仍待定。傾向 LLM 生成時標 bun_type（TurnResult 加欄位，動 engine）。屬 M3.3 + 缺口分析 A2（weird/黃金/腐爛包）。
- **Q3. 撿起來的包子去哪？**
  → 後續方向：M3.4 整理/收藏 + 線索牆/回顧（呼應 BACKLOG F3）。讀完不消失（滿地包子是設計的一部分）。
- **Q4. 丟包子打貓（純丟、砸貓）還保留嗎？**
  → 現行循環是「丟給貓吃」。砸貓/scared 暫未納入核心循環（meow-scared sprite 暫無用）。
- **Q5. latency 失敗怎麼處理？**
  → 部分接上：DB/連線失敗已加 retry + 友善錯誤（見 BACKLOG）。貓「吃了吐不出」的失敗呈現仍可設計（吐壞包子？）。

### 待補素材清單（原始稿，現況見上方「待補素材」）
貓狀態 sprite（咬/嚼/吐/滿足）、黃金/腐爛包、場景背景、丟/吃/吐串接動畫——多數仍 placeholder。